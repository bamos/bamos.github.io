---
# -*- html -*- mode
layout: default
---
<div class="post">
  <h2>{{page.title}}</h2>
  {{ content }}
</div>

<small class="meta">
  {{ page.date | date:"%d %b %Y" }}
  {% for t in page.tags %}
  {% if forloop.first == true %} | {% endif %}
  <a href='/tags#{{ t | escape }}'>{{ t }}</a>{% if forloop.last == false %}, {% endif %}
  {% endfor %}
</small>

